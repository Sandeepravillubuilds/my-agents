# My Agents

My custom agent instructions, specialized role definitions, and skills.

- 13 AGENTS.md instruction files
- 17 specialized agent role files
- 1 custom skill: career-restart-helper, including its interface metadata

Original folder structure and source contents are preserved. Open the relevant folder in Codex to use its instructions. The custom skill lives in codex-career-restart-training/.agents/skills/career-restart-helper/.

This is a definitions-only backup. Supporting knowledge bases, templates, configurations, scripts, application code, inputs, and generated outputs are not included. Some instructions reference these local resources and original absolute paths; restore those resources separately or adapt the paths when reusing an agent. This snapshot does not automatically sync future edits.
