# Prototype Instructions

## Workgrid AI direction

- This is an original marketing site for an AI agent engineering and consulting company.
- Databricks is a benchmark for enterprise confidence and editorial scale only; never copy its brand assets, text, or code.
- Ground consulting claims in the actual agent projects under `/Users/sandeepravillu/Agents`.
- Preserve the ink, bone, chartreuse, and cobalt visual system unless the user asks for a brand change.
- Use the selected Workgrid AI `Precision Wordmark` brand direction: monoline `W` monogram with gold underline, strong `WORKGRID` wordmark, and quieter `AI` suffix.
- Keep consequential agent actions framed around evidence, permission boundaries, and human approval.
- The primary conversion is a consultation request; the live production form still needs a chosen backend or CRM.

Run the local server yourself and open the preview in the in-app browser. Do not give the user server-start instructions when you can run it.

Before making substantial visual changes, use the Product Design plugin's `get-context` skill when the visual source is unclear or no longer matches the current goal. When the user gives durable prototype-specific design feedback, preferences, or decisions, record them in `AGENTS.md`.

When implementing from a selected generated mock, treat that image as the source of truth for layout, component anatomy, density, spacing, color, typography, visible content, and hierarchy.
